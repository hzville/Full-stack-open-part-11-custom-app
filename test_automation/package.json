{
  "devDependencies": {
    "@playwright/test": "^1.49.1",
    "@types/node": "^22.10.7",
    "typescript": "^5.7.3"
  },
  "scripts": {
    "test": "playwright test",
    "test:report": "playwright show-report",
    "build-backend": "npm run build-ci --prefix ../backend",
    "build-frontend": "npm run build-ci --prefix ../frontend",
    "start-prod-server": "npm run start-prod --prefix ../backend"
  }
}
