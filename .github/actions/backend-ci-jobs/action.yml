name: backend ci pipeline

runs:
  using: "composite"
  steps:
    - name: Install dependencies
      working-directory: ./backend
      run: npm ci
    - name: build backend
      run: npm run build
