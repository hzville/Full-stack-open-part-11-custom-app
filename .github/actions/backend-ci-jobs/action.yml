name: backend ci pipeline

runs:
  using: "composite"
  steps:
    - name: Install dependencies
      working-directory: ./backend
      shell: bash
      run: npm ci
    - name: build backend
      shell: bash
      run: npm run build
